{
  "name": "fund-lab-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd fundLab_front && npm install",
    "dev": "cd fundLab_front && npm run dev",
    "build": "cd fundLab_front && npm run build"
  }
}
